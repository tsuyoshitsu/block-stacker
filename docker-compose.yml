services:
  mvp0:
    build:
      context: .
      dockerfile: Dockerfile
    image: block-stacker:mvp0
    container_name: block_stacker_mvp0
    volumes:
      - ./configs:/app/configs:ro
    command: ["block_stacker.mvp0.demo", "--scenario", "all"]
